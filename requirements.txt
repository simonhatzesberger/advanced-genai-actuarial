# requirements.txt — the single package list for the notebooks of this repository.
#
#     pip install -r requirements.txt
#
# PINNING RULE: every requirement is pinned exactly with "==", never a range.
#   * Scientific and open-weights stack — pinned to the versions Google Colab ships, so that a local
#     machine and Colab behave identically (scientific stack checked 2026-09-15, open-weights stack
#     2026-09-21).
#   * SDKs, Jupyter and utilities — pinned to the release checked on 2026-09-15.
#
# GOOGLE COLAB — do not run the install command above in Colab.
#   Colab already provides numpy, pandas, scipy, scikit-learn, statsmodels, matplotlib, pyarrow,
#   pillow, torch, transformers, peft, datasets and accelerate. Each notebook's setup cell installs
#   only the few packages Colab is actually missing, at the pins below.

# --- Scientific stack (mirrors the Colab runtime) ----------------------------
numpy==2.1.3
pandas==2.2.3
scipy==1.16.3
scikit-learn==1.6.1
statsmodels==0.15.0
matplotlib==3.10.0
pyarrow==23.0.1
pillow==11.3.0

# --- Open-weights models (mirrors the Colab runtime) -------------------------
# GPT-2 small in 01_llm_basics and the LoRA fine-tuning run in 02_llm_techniques run on the machine
# that runs the notebook. On Linux, pip installs the CUDA build of torch by default, a larger
# download; the CPU build is enough (https://pytorch.org/get-started/locally/).
torch==2.11.0
transformers==5.17.0
peft==0.21.0
datasets==4.8.5
accelerate==1.15.0

# --- Model SDKs and orchestration --------------------------------------------
openai==3.14.0
langchain-core==1.6.3  # not imported directly: required by langgraph, pinned for reproducibility
langgraph==1.2.11
langgraph-checkpoint==4.2.0
pydantic==2.13.5
httpx==0.28.1
requests==2.32.5  # downloads of annual reports and data sets in notebook 04
kagglehub==0.3.12  # single photos of the vehicle-damage data set in notebook 05
tiktoken==0.14.0
# python-dotenv: read only by the Case Study 2 app, for its own .env file; no notebook uses it.
python-dotenv==1.2.3

# httpx2 is a separate distribution from httpx and the two coexist: openai 3.x uses httpx2, while
# langchain-core uses httpx. It arrives transitively and is deliberately left unpinned, because its
# version was not re-checked on 2026-09-15. Colab ships httpx2 2.12.0 next to httpx 0.28.1; confirm
# and pin it when the environments are built.

# PyMuPDF is licensed under AGPL-3.0. It is used unmodified as an installed dependency for reading
# PDFs; no PyMuPDF code is redistributed in this repository.
pymupdf==1.28.2

# --- Jupyter ------------------------------------------------------------------
jupyterlab==4.6.3
ipykernel==7.3.0
ipywidgets==8.1.9

# --- Utilities ----------------------------------------------------------------
# NOTE: these two pins were not re-checked during the 2026-09-15 verification pass; confirm them
# when the environments are built.
pyyaml==6.0.2
tqdm==4.67.1

# --- Test runner ----------------------------------------------------------------
# Notebook 06 (Case Study 4) grades every translation by running pytest in a separate process.
pytest==9.1.1

# --- Author's tooling ---------------------------------------------------------
# Not used by any notebook or by the app; participants following the seminar never need these.
nbclient==0.11.0
python-pptx==1.0.2
qrcode==8.2
